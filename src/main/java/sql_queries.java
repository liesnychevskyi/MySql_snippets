public class sql_queries
{
//    show databases; // shows databases on the host
//    show tables;    // shows tables in database
//    show columns from (name table); // shows info about table
//    select (column name) from (table name);
//    select (column name_1), (column name_2) from (table name);
//    select * from (table name);
//    select distinct (column name_1), (column name_2) from (table name); // avoiding duplicate records (just unique)
//
//

}
